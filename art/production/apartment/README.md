# Home scene production images

The owner explicitly instructed “put them in the scenes” on 2026-09-16 after viewing
all six outfit images. This authorizes production use, including images with retained
REVISE notes. It does not make them canonical character references.

The original staging records and reviews remain intact. Production records use distinct
IDs and record this authorization. Original PNG bytes are copied to public/art/apartment
for Vite delivery; hashes match the reviewed source files.

Runtime selection uses clinic.outfit after completed Stage One. Mission home,
homePresentation and homeContact show the pre-Glass House variant. Chapter 3 home,
surveillance and complete show the post-Glass House variant. Other scenes show no home
image. Changing the outfit during preparation updates the image immediately; reload
uses the saved selection. Runtime does not import the production catalog or generation
tooling. No save schema, content version or story logic changes were required.

Validation: 34 focused unit tests and six mobile browser scenarios pass; production
build/typecheck pass. Browser tests verify all six images load, outfit changes, reload
persistence and no horizontal overflow or JavaScript errors.
