# Frozen first day-zero release

This content/state snapshot authenticates schema-3, content-3 saves before the fuller prose revision. Do not apply current changes here. The loader verifies the old snapshot exactly, then replays unchanged decisions with current prose, corrected check-in wording and the witnessed-escort knowledge record. Loading does not overwrite stored data.
