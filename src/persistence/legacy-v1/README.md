# Frozen opening content v1

This is the original content/state implementation, retained solely to authenticate existing saves before migration. Do not apply current prose or rule changes here. `saves.ts` validates the original snapshot against this engine, then replays its unchanged ledger with the current implementation. No legacy UI or prototype save format is included.
